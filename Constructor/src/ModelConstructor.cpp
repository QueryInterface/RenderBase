#include "ModelConstructor.h"

using namespace Constructor;

int ModelConstructor::GetModel(VertexList& out_Vertices)
{
    out_Vertices.clear();
    return 0;
}

bool ModelConstructor::SetPrimitive(PrimitiveType type, int x, int y, int z, ElementDirection direction)
{
	type; 
	x;y;z; 
	direction; 
    return false;
}

// eof