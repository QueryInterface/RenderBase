#include "ObjectConstructor.h"

using namespace Constructor;
bool ObjectConstructor::SetPrimitive(ElementType type, const Vector3D& position, const Vector3D& direction)
{
	type; 
	position; 
	direction; 
    return false;
}

// eof